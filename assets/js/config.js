// Site configuration. Change REPO if you fork or rename the repository.
export const CONFIG = {
  REPO: "seeinglikeacitizen/seeinglikeacitizen.github.io",
  // Name of the issue form in .github/ISSUE_TEMPLATE used by "Report a change".
  REPORT_TEMPLATE: "report-change.yml",
  // Optional: a no-account form (Google Form, Tally, etc.) for people without GitHub.
  // When set, the report dialog shows it as a second option.
  REPORT_FALLBACK_URL: null,
  DATA: "data",
};
